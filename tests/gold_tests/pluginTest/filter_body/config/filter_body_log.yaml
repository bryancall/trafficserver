# Configuration for log-only mode.
rules:
  - name: "xxe_detection_log"
    filter:
      direction: request
      methods: [POST]
      headers:
        - name: "Content-Type"
          patterns:
            - "application/xml"
      body_patterns:
        - "<!ENTITY"
    action: [log]
