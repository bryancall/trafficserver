# Configuration for response body filtering
rules:
  - name: "sensitive_data_leak"
    direction: response
    action: [log]
    status: [200]
    headers:
      - name: "Content-Type"
        patterns:
          - "application/json"
          - "text/html"
    body_patterns:
      - "SSN:"
      - "password:"

