# Configuration for response filtering with status code filter.
# Only inspect 200 OK responses, not 404 or other errors.
rules:
  - name: "status_filter_test"
    filter:
      direction: response
      status: [200]
      body_patterns:
        - "test_pattern"
    action: [log]
